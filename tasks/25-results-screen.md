# 25 — The results screen, checked rather than asserted

Don-chan and the crown were both fixed last round and both are behind
the cache in task 19.

## What it changes

Nothing, unless the crown is still too small once it can be seen at its
real size, in which case it gets bigger.

## How it will be shown to work

A screenshot from a browser that has never seen the old build.
